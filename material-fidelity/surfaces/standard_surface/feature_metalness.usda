#usda 1.0
(
    defaultPrim = "World"
    metersPerUnit = 1
    renderSettingsPrimPath = "/Render/Settings"
    subLayers = [
        @../../_assets/base.usda@
    ]
    upAxis = "Y"
)

def Scope "Looks"
{
    def Material "feature_metalness" (
        customData = {
            dictionary materialFidelity = {
                string source = "materials/surfaces/standard_surface/feature_metalness/feature_metalness.mtlx"
                string sourceMaterial = "M_feature_metalness"
            }
        }
    )
    {
        token outputs:mtlx:surface.connect = </Looks/feature_metalness/surface_shader1.outputs:out>
        token outputs:surface.connect = </Looks/feature_metalness/surface_shader1.outputs:out>

        def Shader "surface_shader1"
        {
            uniform token info:id = "ND_standard_surface_surfaceshader"
            color3f inputs:base_color = (0.95, 0.55, 0.3)
            float inputs:metalness = 1
            token outputs:out
        }
    }
}

over "World"
{
    over "Geometry" (
        prepend apiSchemas = ["MaterialBindingAPI"]
    )
    {
        rel material:binding = </Looks/feature_metalness>
    }
}

over "Render"
{
    over "Settings"
    {
        over "Product"
        {
            token productName = "material-fidelity/surfaces/standard_surface/feature_metalness.exr"
        }
    }
}
