#usda 1.0
(
    defaultPrim = "World"
    metersPerUnit = 1
    renderSettingsPrimPath = "/Render/Settings"
    subLayers = [
        @../../_assets/base.usda@,
        @../../_assets/Geometry/plane.usda@,
        @../../_assets/Lights/dome.usda@
    ]
    upAxis = "Y"
)

def Scope "Looks"
{
    def Material "grid" (
        customData = {
            dictionary materialFidelity = {
                string source = "materials/nodes/grid/grid.mtlx"
                string sourceMaterial = "M_grid"
            }
        }
    )
    {
        token outputs:mtlx:surface.connect = </Looks/grid/surface.outputs:out>
        token outputs:surface.connect = </Looks/grid/surface.outputs:out>

        def Shader "uniform_edf"
        {
            uniform token info:id = "ND_uniform_edf"
            color3f inputs:color.connect = </Looks/grid/NG_grid.outputs:out_color>
            token outputs:out
        }

        def Shader "surface"
        {
            uniform token info:id = "ND_surface"
            token inputs:edf.connect = </Looks/grid/uniform_edf.outputs:out>
            token outputs:out
        }

        def NodeGraph "NG_grid"
        {
            color3f outputs:out_color.connect = </Looks/grid/NG_grid/color_mix.outputs:out>

            def Shader "texcoord"
            {
                uniform token info:id = "ND_texcoord_vector2"
                float2 outputs:out
            }

            def Shader "node_under_test"
            {
                uniform token info:id = "ND_grid_color3"
                float2 inputs:texcoord.connect = </Looks/grid/NG_grid/texcoord.outputs:out>
                float2 inputs:uvtiling = (2, 2)
                bool inputs:staggered = 0
                float inputs:thickness = 0.06
                color3f outputs:out
            }

            def Shader "mask_channel"
            {
                uniform token info:id = "ND_separate3_color3"
                color3f inputs:in.connect = </Looks/grid/NG_grid/node_under_test.outputs:out>
                float outputs:outb
                float outputs:outg
                float outputs:outr
            }

            def Shader "color_mix"
            {
                uniform token info:id = "ND_mix_color3"
                color3f inputs:bg = (0.05, 0.12, 0.35)
                color3f inputs:fg = (1, 0.45, 0.05)
                float inputs:mix.connect = </Looks/grid/NG_grid/mask_channel.outputs:outr>
                color3f outputs:out
            }
        }
    }
}

over "World"
{
    over "Geometry" (
        prepend apiSchemas = ["MaterialBindingAPI"]
    )
    {
        rel material:binding = </Looks/grid>
    }
}

over "Render"
{
    over "Settings"
    {
        uniform int ty:convergedSamplesPerPixel = 4

        over "Product"
        {
            token productName = "material-fidelity/nodes/patterns/grid.exr"
        }
    }
}
