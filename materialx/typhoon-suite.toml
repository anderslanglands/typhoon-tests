[suite]
name = "materialx"

[render]
args = ["--disableCameraLight"]
output_pattern = "materialx.{path}.exr"

[reference]
dir = "reference"
pattern = "{path}_glsl.png"
missing = "allow"

[comparison]
# Add default_flip_threshold once the suite has curated regression thresholds.
